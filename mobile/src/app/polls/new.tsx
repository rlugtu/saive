import { Text, View } from 'react-native';

// Stub — implemented in the next step (Create Poll form + bookmark multi-select).
export default function NewPollScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="font-serif-italic text-muted">Create poll — coming soon.</Text>
    </View>
  );
}
